#.gitignore所在的当前目录定义的忽略规则可在子目录中没有.gitignore文件之时被子目录在git时应用于子目录的忽略规则，.gitignore所在的当前目录常常就为根目录
#忽略所有目录层级下的以.iml结尾的文件
*.iml
#忽略根目录层级下的以.gradle结尾的文件
.gradle
#至少忽略根目录层级（包括当前目录）下的local.properties文件
*/local.properties
#至少忽略根目录/.idea层级（包括当前目录）下的libraries子目录
*/.idea/libraries
#至少忽略根目录/.idea层级（包括当前目录）下的modules.xml文件
*/.idea/modules.xml
#至少忽略根目录/.idea层级（包括当前目录）下的workspace.xml文件
*/.idea/workspace.xml
#至少忽略根目录/.idea层级下的所有目录和文件
*/.idea/*
#至少忽略根目录下的以.DS_Store结尾的文件
*/.DS_Store
#忽略根目录下的build子目录中的文件
/build
#忽略根目录下的captures子目录中的文件
/captures
#忽略根目录下的以.externalNativeBuild结尾的文件
.externalNativeBuild
#忽略所有目录层级下的以.idea结尾的文件
*.idea
#如果当前目录中存在Demo文件夹的话，则忽略路径Demo/app/build子目录中的文件
Demo/app/build
#如果当前目录中存在TUIKit文件夹的话，则至少忽略路径TUIKit/*/*/build子目录中的文件
TUIKit/*/*/build
